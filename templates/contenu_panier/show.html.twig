{% extends 'base.html.twig' %}

{% block title %}{{'base.cart-contents'|trans}}{% endblock %}

{% block body %}
    <h1>{{'base.cart-contents'|trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contenu_panier.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ contenu_panier.quantite }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ contenu_panier.date ? contenu_panier.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contenu_panier_index') }}">{{'back'|trans}}</a>

    <a href="{{ path('app_contenu_panier_edit', {'id': contenu_panier.id}) }}">{{'edit'|trans}}</a>

    {{ include('contenu_panier/_delete_form.html.twig') }}
{% endblock %}
